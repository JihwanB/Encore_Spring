<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.encore_spring_pjt.mapper.BoardMapper">

    <sql id="insertColumns">
        title       ,
        content     ,
        writer      ,
        view_cnt    ,
        notice_yn   ,
        secret_yn   ,
        delete_yn   ,
        insert_time ,
        update_time
    </sql>
    <sql id="selectColumns">
        idx,
        title       ,
        content     ,
        writer      ,
        view_cnt    ,
        notice_yn   ,
        secret_yn   ,
        delete_yn   ,
        insert_time ,
        update_time
    </sql>

    <insert id="save"
            parameterType="com.example.encore_spring_pjt.domain.BoardRequest">
        insert into encore_board_tbl(<include refid="insertColumns"/>)
        values(
               #{title},
               #{content},
               #{writer},
               0,
               #{noticeYn},
               #{secretYn},
               false,
               Now(),
               NULL)
    </insert>

    <select id="findByIdx"
            parameterType="com.example.encore_spring_pjt.domain.BoardRequest"
            resultType="com.example.encore_spring_pjt.domain.BoardResponse">
        select <include refid="selectColumns"/>
        from encore_board_tbl
        where idx = #{idx}
    </select>

</mapper>